name: business-card
services:
  business-card:
    build: .
    image: business-card:${RELEASE_TAG:-local}
    restart: unless-stopped
    ports:
      - "127.0.0.1:${PREVIEW_PORT:-8080}:80"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
