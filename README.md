# Визитка Дмитрия Кошелева

Визитка в корпусе испытательного терминала поверх настоящей 3D-камеры, вдохновлённой Portal 2 и приложенным референсом. В камере есть плитка, стеклянные перегородки, механические панели, кнопки, куб, обломки, лианы и табло. Геометрия и рисованные текстуры панелей созданы для проекта; материалы бетона и металла взяты из ambientCG (CC0). Это самостоятельная интерпретация, а не оригинальная карта Valve.

На ПК сцена включена по умолчанию. На сенсорном устройстве или при ширине до 700 px сцена выключена: модуль 3D и его текстуры не загружаются до нажатия «3D». При отключении останавливается отрисовка, вместо неё виден статичный кадр той же сцены. В скрытой вкладке отрисовка приостанавливается.

Кнопки вне визитки: 3D, скрыть/показать визитку и поворот на 180°. Escape возвращает скрытую карточку. Мобильный поворот на 90° сохранён. Смена разделов переворачивает отдельные сегменты корпуса каскадом и запускает механические панели в комнате. При уменьшении движения в ОС переход и дрейф камеры отключаются.

## Файлы

| Файл | Назначение |
| --- | --- |
| `index.html` | Тексты, контакты, четыре раздела и внешние кнопки |
| `css/main.css` | Светлый терминал, адаптивная геометрия, flip-переход |
| `js/main.js` | Разделы, история, часы МСК, управление сценой, загрузка по требованию |
| `src/chamber.js` | Three.js: материалы, меши, освещение, тени, растения, движение камеры |
| `js/chamber.js` | Готовая локальная сборка сцены, включена в Git и Docker |
| `img/scene-textures/` | Карты цвета, нормалей (рельефа), шероховатости и затенения |
| `img/scene-poster.webp` | Снимок этой же сцены для режима 3D OFF |
| `img/optimized/`, `img/social/` | Изображения визитки и существующие иконки |
| `scripts/build.cjs` | Сборка сцены через esbuild |
| `scripts/serve.cjs` | Локальный HTTP preview с тем же CSP, что у Nginx |
| `scripts/test-browser.cjs`, `scripts/test-scene.cjs` | Проверки интерфейса и WebGL через Playwright |
| `THIRD_PARTY.md` | Источники материалов и лицензии |

## Локальный просмотр

Готовая сцена уже собрана. Достаточно Node.js:

```sh
node scripts/serve.cjs
```

Адрес: http://127.0.0.1:4173. Нужен HTTP-сервер: открытие HTML через `file://` не поддерживает загрузку модуля сцены.

После изменения `src/chamber.js`:

```sh
npm install
npm run build
npm run preview
```

Three.js и esbuild нужны при пересборке. Production отдаёт готовые локальные файлы без CDN. После изменения обычного HTML/CSS/`js/main.js` пересборка не нужна.

Запуск с Docker:

```sh
docker compose up -d --build --wait
```

Адрес: http://localhost:8080. Порт задаётся через `PREVIEW_PORT`.

## Редактирование

Контакты и тексты находятся в `index.html`; изображения работ переключаются через `data-image` у `<details>`. Социальные ссылки пока имеют `href="#"`: подставь свои реальные адреса.

Комната генерируется детерминированно из геометрии Three.js. Размеры, расположение объектов и параметры света находятся в `src/chamber.js`. После изменения сцены нужно пересобрать `js/chamber.js`; статичный `scene-poster.webp` обновляется отдельным снимком сцены без интерфейса.

Максимальная плотность рендера — 1.5; статичная архитектура объединяется по материалу, листва использует InstancedMesh. Выключенная сцена перестаёт рисовать кадры, ресурсы остаются в памяти для повторного включения. При отсутствии WebGL2 визитка работает на статичном фоне.

## Как устроен сервер

```text
Интернет -> Caddy (80/443) -> Docker-сеть web-edge -> Nginx визитки (80)
```

Внешние 80/443 занимает только Caddy. Порт 8080 визитки привязан к localhost сервера, извне он закрыт. Постоянные Docker volumes `web-edge_caddy_data` и `web-edge_caddy_config` сохраняют сертификаты и служебные данные. `restart: unless-stopped` поднимает контейнеры после перезагрузки сервера. Логи ограничены по размеру.

## Первое развёртывание на чистом сервере

Установку Docker выполняй только на подходящем Ubuntu-сервере от root:

```sh
bash deploy/install-docker.sh
docker network inspect web-edge >/dev/null 2>&1 || docker network create web-edge
```

Помести исходники в `/opt/business-card` (архив или Git после публикации ветки), затем:

```sh
cd /opt/business-card
bash deploy/update.sh
mkdir -p /opt/web-edge/conf
cp deploy/edge/compose.yml /opt/web-edge/compose.yml
cp Caddyfile /opt/web-edge/conf/Caddyfile
cd /opt/web-edge
docker compose run --rm --no-deps caddy caddy validate --config /etc/caddy/Caddyfile
docker compose up -d
```

Копирование Caddyfile здесь — только для первого запуска. При наличии других сайтов редактируй существующий общий `/opt/web-edge/conf/Caddyfile`, сохраняя их блоки.

## Домен и автоматический HTTPS

Порты 80 и 443 должны быть доступны снаружи. Caddy автоматически запрашивает сертификат и продлевает его заранее, пока запущен, доступен интернет и корректен DNS. Отдельный cron/Certbot не нужен. Данные сертификатов нельзя удалять командой `docker compose down -v`.

Проверки:

```sh
cd /opt/web-edge
docker compose logs --tail=60 caddy
curl -I https://dkoshelev.resbook.ru/
```

## Обновление и откат

1. Сохрани текущий образ для отката:

```sh
docker tag business-card:local business-card:previous
```

2. Передай обновлённые файлы в `/opt/business-card`, затем:

```sh
cd /opt/business-card
bash deploy/update.sh
curl -I http://127.0.0.1:8080/
```

3. Проверь сайт в браузере. Если нужно откатить именно работающий контейнер:

```sh
cd /opt/business-card
RELEASE_TAG=previous docker compose -f docker-compose.yml -f compose.production.yml up -d --no-build --wait
```

Файлы исходников при откате образа остаются новыми: перед следующей сборкой исправь их или восстанови нужную версию из Git. Команда сборки снова применит файлы с диска.

Для обновления базового Nginx после проверки релиза:

```sh
cd /opt/business-card
docker compose -f docker-compose.yml -f compose.production.yml build --pull
docker compose -f docker-compose.yml -f compose.production.yml up -d --wait
```

Для обновления общего Caddy: `docker compose pull caddy` и `docker compose up -d caddy` в `/opt/web-edge`. Это касается всех проектов за прокси, поэтому планируй короткий перезапуск. Используются каналы `nginx:stable-alpine` и `caddy:2-alpine`; контейнеры сами не обновляют образы.

## Добавление других микропроектов

У каждого проекта свой Compose и уникальный сетевой псевдоним. Подключи его к внешней сети `web-edge` (как в `compose.production.yml`) и добавь блок нового домена в общий Caddyfile:

```caddyfile
project.example.ru {
    reverse_proxy unique-project-name:3000
}
```

Здесь `3000` — внутренний порт приложения. Не публикуй ему 80/443 на хосте и не запускай второй Caddy. После добавления DNS проверь и перезагрузи общий Caddyfile командами выше. Базы данных не подключай к общей сети прокси: им нужна отдельная сеть своего проекта.

## Индексация

HTML и HTTP-заголовок содержат `noindex, nofollow, noarchive, noimageindex`. В `robots.txt` обход разрешён специально: поисковик должен прочитать запрет индексации. `Disallow: /` мог бы скрыть от него этот запрет. Это не пароль и не гарантия против недобросовестных роботов. Уже существующие результаты поиска исчезают после повторного обхода.


## Проверки

Нужны Node.js, Playwright и Edge. После `npm install`:

```sh
npm test
```

Первый тест проверяет шесть размеров: 1440x900, 1024x768, 390x844, 320x568, 844x390, 768x1024. Проверяет границы карточки и всех кнопок, разделы, проекты, поворот, историю, клавиатуру и ошибки HTTP/JS.

Второй тест проверяет реальный WebGL, запуск/остановку, отсутствие загрузки 3D на телефоне до включения, скрытие карточки, Escape, быстрые flip-переходы, уменьшение движения и fallback при недоступном WebGL. Снимки сохраняются в `.local/`.

`BROWSER_CHANNEL` меняет браузер (по умолчанию `msedge`); `TEST_URL` в первом тесте позволяет проверить опубликованный сайт.
